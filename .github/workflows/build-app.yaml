name: build-app

on:
  pull_request:
    branches:
      - "main"
    types: [closed]

jobs:
  build:
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest

steps:
  - name: build for production
    run: pnpm prod
  - name: success message
    run: echo "Build success fully"
